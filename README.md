# oldmerman-assistant

**oldmerman-knowledge** 的前端实现。基于 Vue 3 + TypeScript + Vite 构建的知识库管理 SPA。

## 开发命令

```sh
pnpm install       # 安装依赖
pnpm dev       # 启动开发服务器（http://localhost:5173）
pnpm run build     # 类型检查 + 生产构建
```

## 联调
后端的URL位置：`src\utils\http.ts`中的`API_BASE_URL`